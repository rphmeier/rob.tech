version: 1
frontend:
  phases:
    preBuild:
      commands:
        - bundle install
    build:
      commands:
        - bundle exec jekyll build
  artifacts:
    files:
        - **/*
    baseDirectory: _site
